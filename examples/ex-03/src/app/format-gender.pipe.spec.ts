import { FormatGenderPipe } from './format-gender.pipe';

describe('FormatGenderPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatGenderPipe();
    expect(pipe).toBeTruthy();
  });
});
